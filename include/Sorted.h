#ifndef SORTED_H
#define SORTED_H

#include <Numbers.h>

class Sorted : public Numbers
{
    public:
        Sorted();
        virtual ~Sorted();
        void generateNumbers(int numVal);
    protected:
    private:
};

#endif // SORTED_H
